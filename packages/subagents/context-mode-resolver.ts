/** Compatibility exports for Subagents. Context Mode resolution is owned by the Context Mode package. */
export {
  CONTEXT_MODE_PACKAGE,
  CONTEXT_MODE_VERSION,
  contextModeAvailable,
  contextModeRoot,
  contextModeServer,
  contextModeVersion,
} from "../context-mode/resolver.js";
